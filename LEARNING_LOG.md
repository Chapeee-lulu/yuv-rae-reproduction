# 共同复现学习日志

这个文件记录“我们做了什么”和“你是否真正理解”。每个阶段完成后再提交到GitHub。

## 协作规则

每一步固定回答六个问题：

1. 论文在这一步要解决什么问题？
2. 作者开源代码使用了哪个文件、函数和关键参数？
3. 作者代码的大概思路是什么？
4. 我们如何不复制源码、独立实现同一功能？
5. 用什么测试证明我们的实现正确？
6. 你能否用自己的话解释输入、输出、变量和结果？

你的理解检查未完成时，该阶段只能标记为“代码已实现”，不能标记为“本人已掌握”。

## Step 01：RGB↔YUV颜色空间转换

### 论文对应内容

- Fig. 2–3；
- 公式(13)、(14)；
- 方法第3.1节。

### 作者代码位置

- `官方仓库/utils.py:3`：面向整数图像的`rgb2yuv`；
- `官方仓库/utils.py:20`：面向整数图像的`yuv2rgb`；
- `官方仓库/atk.py:5`：面向PyTorch张量和梯度计算的`rgb2yuv2`；
- `官方仓库/atk.py:16`：面向PyTorch张量的`yuv2rgb2`。

### 作者代码的大概思路

作者实际上使用了两套颜色转换：

1. 生成攻击梯度时，使用0–1范围的PyTorch张量，保留计算图，使梯度能够传回Y通道；
2. 嵌入和保存图像时，转为0–255范围的NumPy数组，并对Y/U/V取整。

### 我们怎么复现

- 只依据论文公式独立实现`src/yuv_rae/yuv_conversion.py`；
- 明确区分浮点转换和整数取整转换；
- 不导入作者仓库中的任何模块；
- 使用作者数据集的一张PNG作为只读测试输入。

### 如何验证

- 单元测试验证输入形状和典型颜色；
- 对真实图像统计逐像素误差；
- 同时报告浮点往返和整数往返结果。

### 当前结果

- 单元测试：3/3通过；
- 测试图：`0001_321.png`，299×299；
- 浮点往返最大绝对误差：约0.00948；
- 整数往返最大绝对误差：1；
- 整数往返并非逐像素完全一致；
- 64851/89401个像素至少有一个通道变化，占72.54%。

### 目前能得出的结论

论文公式加普通整数取整不能单独保证RGB逐像素无损。这不等同于已经否定论文的error-free结论；仍需确定论文的恢复基准、PEE恢复对象及作者实际比较方式。

### 你的理解检查

请在下一次提交前，用自己的话回答：

1. 为什么攻击阶段需要可求梯度的浮点转换，而不能直接使用NumPy取整？
2. YUV 4:4:4没有色度下采样，为什么仍可能产生±1像素误差？
3. “SSIM=1.000”和“所有RGB像素完全相等”有什么区别？

理解状态：**等待你的回答**。
